const Categories = (category) => {
  return `<button id="${category}-search-button" class="category-search-button search-button button button-primary">${category}</button>`;
};

export default Categories;
